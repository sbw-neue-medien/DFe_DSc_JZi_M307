# Beispiel-Umgebungsvariablen für die Docker-Umgebung

# Datenbankverbindung
DB_HOST=db
DB_USER=root
DB_PASS=example
DB_NAME=m307_projektmanagement
DB_PORT=3306

# Passwort für den MySQL root-Benutzer (wird von docker-compose gelesen)
MYSQL_ROOT_PASSWORD=example
